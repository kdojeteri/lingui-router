
describe('extract plugin', () => {
  it('should turn i18nTo tagged template strings into corresponding marked catalog strings');

  it('should turn i18nTo function calls into corresponding marked catalog strings');

  it('should fail if i18nPath tagged template literal is used with more than one quasi part');

  it('should turn i18nPath tagged template strings into corresponding marked catalog strings');

  it('should turn i18nPath function calls into corresponding marked catalog strings');
});
